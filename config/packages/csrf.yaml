# CSRF basato su sessione (non stateless): funziona senza JS e resta isolato
# per-app grazie al nome cookie di sessione univoco (vedi framework.yaml).
framework:
    csrf_protection:
        enabled: true
